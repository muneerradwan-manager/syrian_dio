# syrian_dio

Production-ready networking layer for Flutter apps built on Dio:
- Result<T> (no exceptions flow)
- Unified error mapping
- Retry with exponential backoff
- Auth token injection
- Optional refresh token flow (single refresh + queued requests)
- Logging

## Install
```yaml
dependencies:
  syrian_dio: ^0.1.0
